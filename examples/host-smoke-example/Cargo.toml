[package]
name = "host-smoke-example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hibana = { version = "0.4.1", default-features = false }
hibana-pico = { path = "../.." }

[features]
default = []
wasm-engine-core = ["hibana-pico/wasm-engine-core"]
